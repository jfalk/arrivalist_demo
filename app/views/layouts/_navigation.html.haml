%nav.top-bar
  %ul.title-area
    %li.name
      %h1= link_to 'Arrivalist Demo', root_path

  %section.top-bar-section
    %ul.right
      %li
        = current_user ? link_to('Logout', signout_path, method: :delete) : link_to('Login', signin_path)